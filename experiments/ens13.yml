experiment_name: 'ens13'

model: CNN

data:
  data_folder: 'TCSA_data'
  batch_size: 50
  encode_length: 1
  estimate_distance: 8
  rotate_type: 'no'
  input_image_type: [2]

training_setting:
  evaluate_freq: 1
  class_weight:
    - 1
  learning_rate: 0.0005
  max_epoch: 30

 
